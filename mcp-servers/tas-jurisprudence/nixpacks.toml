# Nixpacks configuration for TAS/CAS Jurisprudence MCP Server
# Installs Playwright Chromium browser for JavaScript rendering

[phases.setup]
nixPkgs = ['...', 'chromium', 'chromium.sandbox']

[phases.install]
cmds = ['npm ci', 'npx playwright install chromium']

[start]
cmd = 'node dist/index.js'
